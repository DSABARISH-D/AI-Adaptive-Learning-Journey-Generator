name: CI/CD Pipeline - AI Adaptive Learning Journey Generator

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  backend-qa:
    name: Backend Lint, Type Check & Test Suite
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Lint with Ruff
        run: |
          cd backend
          ruff check .

      - name: Run Backend Pytest Suite (Unit + Integration + AI Mastery Tests)
        run: |
          cd backend
          pytest -v --tb=short

  frontend-qa:
    name: Frontend Lint, Typecheck, Test & Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Run ESLint
        run: |
          cd frontend
          npm run lint

      - name: Run Vitest Suite
        run: |
          cd frontend
          npm run test -- --run

      - name: Build Frontend Bundle (TypeScript & Vite)
        run: |
          cd frontend
          npm run build

  docker-build-verification:
    name: Docker Build & Security Scan
    needs: [backend-qa, frontend-qa]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Backend Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: false
          tags: adaptive-learning-backend:ci-test

      - name: Build Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: false
          tags: adaptive-learning-frontend:ci-test
